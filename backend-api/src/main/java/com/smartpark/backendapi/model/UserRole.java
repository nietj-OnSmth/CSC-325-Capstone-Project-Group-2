package com.smartpark.backendapi.model;

/**
 * Enum representing the type of user accessing the parking system.
 * Enum representing the different roles a user can have in the system.
 * These roles determine access to specific features and parking lots.
 */
public enum UserRole {
    STUDENT,
    FACULTY,
    ADMIN
}

